{{!-- edit
    <div class="container">
        <h1 class="mb-1">Edit Post </h1>
        <form action="/post/{{post.id}}?_method=PUT" method="POST">
            <div class="form-group">
                <label for="title mb-2">New Title</label>
                <input required type="text" name="title" class="form-control" value="{{ post.title }}">
            </div>
            <div class="form-group">
                <label for="description mb-2"> New Description</label>
                <textarea name="description" id="description"
                    class="form-control mb-2">{{ post.description }}</textarea>
            </div>
        </form>

        <a href="/post/edit/{{post.id}}" class="btn btn-info" value="edit">Edit</a>
    </div> --}}
